{
  "name": "incisio-web",
  "version": "2.0.0",
  "description": "Incisio — Surgical Procedure Logbook",
  "private": true,
  "scripts": {
    "dev": "npx serve public",
    "build": "echo 'No build step required'",
    "start": "npx serve public"
  },
  "dependencies": {},
  "devDependencies": {
    "serve": "^14.0.0"
  }
}
